#include <bits/stdc++.h>
using namespace std;
int main() 
{
    int a,b,n, d, c=0,t,s=0;
    cout<<"Enter the first number of the range: ";
    cin>>a;
    cout<<"Enter the last number of the range: ";
    cin>>b;
    for(int i=a;i<=b;i++)
    {
    c=0;
    s=0;
    n=i;
    t=n;
    while(n>0)
    {
        d=n%10;
        c++;
        n=n/10;
    }
    n=t;
    while(n>0)
    {
        d=n%10;
        s=s+pow(d,c);
        n=n/10;
    }
    if(s==t)
    cout<<t<<" is armstrong number\n";
    }
return 0;
}
