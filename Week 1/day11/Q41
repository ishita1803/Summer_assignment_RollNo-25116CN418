#include <bits/stdc++.h>
using namespace std;

int sum(int a, int b)
{
    int s=a+b;
    return s;
}
int main()
{
    int a,b;
    cout<<"Enter the first number: ";
    cin>>a;
    cout<<"Enter the second number: ";
    cin>>b;
    cout<<"Sum: "<<sum(a,b);
    return 0;
}
