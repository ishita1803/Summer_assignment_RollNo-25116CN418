#include <bits/stdc++.h>
using namespace std;
int main() 
{
    int n,s=1;
    cout<<"Enter the number: ";
    cin>>n;
    for(int i=2;i<n;i++)
    {
        if(n%i==0)
        s=s+i;
    }
    if(s==n)
    cout<<n<<" is a perfect number";
    else
    cout<<n<<" is not a perfect number";77
    
return 0;
}
